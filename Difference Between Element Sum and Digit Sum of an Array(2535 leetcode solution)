class Solution {
public:
    int differenceOfSum(vector<int>& nums) {
        int n=nums.size();
        int sum=0;
        for(int i=0;i<n;i++){
            sum+=nums[i];
        }
        int s=0;
        for(int i=0;i<n;i++){
            int temp=nums[i];
            while(temp>0){
                temp=temp/10;
                s+=digit;
            }
        }
         int diff=abs(sum-s);
        return diff;
    }
};
